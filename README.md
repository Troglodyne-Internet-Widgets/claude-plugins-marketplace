# claude-plugins-marketplace

Troglodyne clod LART application necronomicon

Run `claude plugin marketplace add Troglodyne-Internet-Widgets/claude-plugins-marketplace` to get your grubby mitts on my secrets

You can also use the slopped script `claude.sh` as a wrapper for claude.

I hate that it drops to interactive and wastes tokens when you just pass unrecognized args, so this script prints usage instead like a good program would.
